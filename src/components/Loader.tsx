import { ClockLoader } from "react-spinners";

const Loader = () => {
  return (
    <div className="d-flex justify-content-center align-items-center my-5">
      <ClockLoader color="dodgerblue" />
    </div>
  );
};

export default Loader;
